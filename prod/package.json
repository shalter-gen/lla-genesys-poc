{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": { 
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "build": "echo 'No build step required'"
  },
  "engines": {
    "node": ">=16.0.0 <18"
  }
}